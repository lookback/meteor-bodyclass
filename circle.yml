dependencies:
  override:
    - curl https://install.meteor.com | /bin/sh
    - meteor build:
        pwd:
          "test-app"

test:
  override:
    - meteor --test:
        pwd:
          "test-app"
